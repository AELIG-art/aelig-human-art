# World Service

This service will create and verify World ID requests and produce narrowly scoped artist-authorization evidence.

It must bind the exact operation, expected credential, environment, action, context, nonce, and expiry. It must not expose RP signing material to the browser or convert a client-side success callback into authorization.

Implementation status: not started.

