# Circuits

This package will contain versioned cryptographic circuits, public input schemas, synthetic fixtures, and verifier-generation instructions.

Do not add operational witnesses, tag keys, or setup artifacts. The selected proof system, key commitment, and setup procedure remain open decisions until feasibility work passes the documented validation gates.

Implementation status: not started.
